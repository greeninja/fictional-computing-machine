<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <%= render "form_error", form: user %>
  <% end %>
    <%= render(:partial => "rats", :locals => {:f => f}) %>
    <%= f.submit :Submit, :class => "btn btn-success" %>
<% end %>
